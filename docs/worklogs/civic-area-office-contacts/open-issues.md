# Open Issues

- Hosted Supabase still requires the focused SQL Editor artifact or normal migration deployment.
- A public office contact proves published provenance, not mailbox/switchboard responsiveness.
- Coverage outside verified current jurisdiction geometry remains unsupported.
- The repository master SQL artifact must be regenerated by the coordinating session. Generated
  database types should remain unchanged because the function signature and relational return type
  did not change, but the standard type check must confirm that assumption.
