# Open issues

## Hosted activation

The repository cannot apply the forward migration or generated taxonomy seed to hosted Supabase
without the reviewed deployment workflow. Apply the supplied SQL Editor artifact only after the
target migration state is reconciled, then smoke the catalog, draft resume and one mapped
submission.

The target must already contain the existing twelve V1 operational profiles and migration
`20260723110000_prune_deferred_v1_subsystems.sql`. SQL Editor execution does not update the
Supabase CLI migration ledger.

## Pending route crosswalks

Most taxonomy subcategories intentionally remain pending until source-backed authority,
jurisdiction, department/role, asset and fallback records exist. Their presence in the catalog is
not approval to route them.

Current count: 327 pending subcategories. Thirteen entries map to twelve stable operational
profiles.

## Protected corruption intake

Corruption submission remains unavailable until an independent competent oversight destination,
accused-chain exclusion, secure contact channel, confidentiality/whistleblower policy, evidence
retention, access logging and escalation procedure are approved. It must never use the ordinary
ward email as a placeholder.

## Device validation

Run a physical-device pass for long labels, screen-reader focus, Android back behavior, keyboard
interaction, draft resume and disabled/pending selection explanations.
