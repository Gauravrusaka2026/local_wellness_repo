# Phase 8 Open Issues

- Approve operational public visibility, sensitivity, generalization, aggregation, moderation,
  withdrawal, retention, and abuse-response policy (`TRANSPARENCY-001`).
- Build the authorized reviewer workflow/API for creating and withdrawing duplicate groups before
  production moderation; the database capability is intentionally service-only and no automatic
  similarity result is publishable.
- Select and review a basemap provider and outbound-coordinate policy (`TRANSPARENCY-002`).
- Import reviewed pilot ward geometry (`DATA-004`).
- Complete processed-media decoding, malware/moderation, redaction, cleanup, and delivery policy
  before exposing derivatives (`GOVDASH-002`).
- Keep public comments disabled until the separate moderation and abuse-control decision
  (`NOTIFY-003`).
